{
  "name": "calo-task",
  "version": "1.0.0",
  "description": "This is a simple Node.js application using Fastify framework to manage jobs data.",
  "main": "index.js",
  "scripts": {
    "install:all": "npm install --prefix ./server && npm install --prefix ./client",
    "start:all": "concurrently \"npm run start --prefix ./server\" \"npm run dev --prefix ./client\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
